---
interface Props {
  navigation: Array<{ label: string; href: string }>;
}
const { navigation } = Astro.props;
const currentPath = Astro.url.pathname;
---

<header class="site-header">
  <div class="shell header-inner">
    <a class="brand" href="/" aria-label="XeDotNet, pagina iniziale">
      <img class="brand-logo" src="/images/xe-logo-original.png" alt="XE" width="162" height="97" />
      <span class="brand-name">Development user group</span>
    </a>
    <nav aria-label="Principale">
      <ul class="nav-list" role="list">
        {navigation.map((item) => (
          <li>
            <a href={item.href} aria-current={currentPath.startsWith(item.href) && item.href !== '/' || currentPath === item.href ? 'page' : undefined}>
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
    <div class="theme-control" hidden>
      <label for="theme">Tema</label>
      <select id="theme" name="theme">
        <option value="system">Sistema</option>
        <option value="light">Chiaro</option>
        <option value="dark">Scuro</option>
      </select>
    </div>
  </div>
</header>
